provider_installation {
  filesystem_mirror {
    # Путь должен указывать на корень папки с плагинами
    path    = "/var/jenkins_home/.terraform.d/plugins"
    include = ["yandex-cloud/yandex"]
  }
  direct {
    exclude = ["yandex-cloud/yandex"]
  }
}
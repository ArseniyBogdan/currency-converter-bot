all:
  children:
    bot_vms:
      hosts:
        currency_converter_bot:
          ansible_host: VM_PRIVATE_IP  # Подставьте вывод server_private_ip из Terraform
          vars:
            ansible_user: ubuntu
            ansible_ssh_private_key_file: ~/.ssh/id_rsa